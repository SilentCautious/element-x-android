<?xml version="1.0" encoding="utf-8"?><!--
  ~ Copyright (c) 2025 Element Creations Ltd.
  ~ Copyright 2023-2025 New Vector Ltd.
  ~
  ~ SPDX-License-Identifier: AGPL-3.0-only OR LicenseRef-Element-Commercial.
  ~ Please see LICENSE files in the repository root for full details.
  -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The provider subscribes to the ntfy topic with a WebSocket hosted by the app process. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
    Required to run the service keeping the connection alive. `remoteMessaging` is the type fitting a
    long-lived connection waiting for incoming messages; it is only known from Android 14 onwards and
    older versions simply ignore the permission.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />

    <application>
        <service
            android:name=".keepalive.NtfyKeepAliveService"
            android:exported="false"
            android:foregroundServiceType="remoteMessaging" />
    </application>
</manifest>
